// Stats dashboard component stub: displays live telemetry readings (FPS, latency, streak count).
export class StatsComponent {
  render(): void {
    console.log('Rendering Stats Component (placeholder)');
  }
}
